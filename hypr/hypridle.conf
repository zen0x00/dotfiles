general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on && pidof quickshell || uwsm-app -- quickshell
}

# Lock after 5 minutes
listener {
    timeout = 300
    on-timeout = loginctl lock-session
}
